@model MVCandSQLCONNECTION.Models.OswalList
@using System.Data;
@{
    ViewBag.Title = "Listing";
}


<link href="~/Content/bootstrap.min.css" rel="stylesheet" />

<a href="@Url.Action("Index")" class="btn btn-primary">Add</a>
<br />
@TempData["Added"]
<br />
@TempData["Delete"]
<br />
@TempData["Edit"]
<br />
<main>
    <table class="table">
        <thead>
            <tr>
                @foreach (DataColumn dc in Model.Otable.Columns)
                {
                    <th scope="col">
                        @dc.ColumnName
                    </th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (DataRow dr in Model.Otable.Rows)
            {
                <tr>
                    @foreach (DataColumn dc in Model.Otable.Columns)
                    {
                        <td>
                            @dr[dc.ColumnName]
                        </td>
                    }
                    <td><a href="@Url.Action("Delete", new { Id = dr["OswalId"] })"> Delete </a></td>
                    <td><a href="@Url.Action("Edit", new {Id = dr["OswalId"] })"> Edit </a></td>
                </tr>
            }
        </tbody>
    </table>
</main>
